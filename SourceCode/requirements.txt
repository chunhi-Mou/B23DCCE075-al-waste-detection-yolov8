# Pinned to the locked compute env (CLAUDE.md §7/§8): local Apple M4, MPS.
# Python 3.14.4. Pin-forward 2026-05-18 (author): matches the env every
# baseline/AL run executes on — supersedes the stale py3.11/np1 pin.
ultralytics==8.4.51
torch==2.12.0
torchvision==0.27.0
numpy==2.4.4
pyyaml==6.0.3
opencv-python-headless==4.13.0.92
matplotlib==3.10.9
pytest==9.0.3
gradio==6.15.2
